## [2026-08-12] 专题：通用批量训练与推理数据输入输出及用户配置框架规范说明书（通用抽象版）
- **类型**：通用架构规范 / 数据接口与用户配置中心技术标准说明书
- **目标与假设**：
  - **目标**：抽离具体业务算法细节，针对多对象、多任务形态下的**批量训练与推理数据处理架构**，提炼通用数据输入输出结构、多层级配置规范（JSON Schema）、断点续跑状态模型以及临时产物隔离清理契约。
  - **假设**：各类具备时间序列或多通道输入的数据集经过编排层 `run_batch_users.py` 与流水线引擎 `run_user_pipeline.py` 的协同，实现对齐、特征生成、独立模型训练、残差校正学习、OOD与数据泄漏审计及业务周期评估的闭环。
- **方法 / 数据 / 参数**：
  - **方法**：系统化提炼批量编排层、单任务控制流水线及底层配置解析中心（`time_filter_utils.py`）的通用工程接口；分离“算法私有数据”与“通用框架架构数据”。
  - **数据**：涵盖 `data/` 根目录双模式输入组织（规范双分层 vs 平铺自动推导）、`models/` 资产打包规范及 `artifacts/` 各运行产物的通用分层架构。
  - **参数**：提炼通用配置文件中适用的六大功能模块（全局时段过滤、复合目标表达式、自适应算法守卫、三区独立样本过滤、通用常量覆盖与环境映射、架构进阶开关）及三层解析优先级链。
- **结果 / 结论**：
  - 形成了《通用批量训练与推理数据输入输出及用户配置框架规范说明书（通用抽象版）》，确立了高通用的时序批处理文件体系、标准评估体系（包含 25 列带原始点计数的监控表）、时间段审计表及断点续跑 9 列原子状态规范。
  - 明确了工程开发中**白名单隔离保护契约 (`_CLEANUP_WHITELIST`)**与**配置异常降级回退 (`WARN + Fallback`)**两项基本保障机制。
- **是否进入 REPORT.md（稳定结论）**：否（本说明书专注于系统通用数据输入输出与用户配置规范的标准化建档，沉淀于 `REPORT_TEST.md`；具体业务领域的算法实验与演进见 `REPORT.md`）。
- **遗留问题**：
  - 随着框架后续功能演进，如有新增通用控制属性或产物，须按本说明书约定的命名、数据类型与回退链路规范在此追加更新。

---

# 《通用批量训练与推理数据输入输出及用户配置框架规范说明书（通用抽象版）》

> **制定日期**：2026-08-12  
> **文档定位**：面向批量训练与推理服务框架，剥离具体业务领域的专业物理概念与特定模型细节，全面规范**数据输入接口与文件组织**、**标准产物输出与监控结构**、**集中式配置参数体系（JSON Schema）**以及**工程保护与状态恢复机制**。

---

## 第一部分：框架设计宗旨与层级控制架构

本框架基于“**批量编排调度**”与“**单任务执行流水线**”二层解耦架构设计，旨在给时序多任务学习及预测系统提供通用数据加载、按需时间过滤、模型持久化配置、产物标准化汇总与安全隔离能力。

```
+-----------------------------------------------------------------------------------------------+
|                             批量调度层 (run_batch_users.py)                                   |
|  - 扫描工作目录 (data/) 并解析被管理对象 (User/Device) 的输入文件链                            |
|  - 解析集中式 JSON 配置文件，按用户层/兜底层形成参数结构并传递                                |
|  - 原子化维护 9 列标准状态表 (batch_execution_state.csv)，支持 --resume 断点续跑               |
+-----------------------------------------------------------------------------------------------+
                                               |
         (通过序列化 JSON CLI 参数 --time-filter-spec 及环境变量 NILM_USER_* 进行环境封堵与透传)
                                               v
+-----------------------------------------------------------------------------------------------+
|                            单任务流水线引擎 (run_user_pipeline.py)                             |
|  - 安全清理历史垃圾，执行 _CLEANUP_WHITELIST 顶层白名单保护                                   |
|  - 串行调度基础分析与算法构建标准流程：                                                       |
|       [Step 01] 数据勘察与健康度检查 (审计输入采样点密度与时间覆盖度)                        |
|       [Step 02] 统一时间序列网格重采样与多通道对齐 (生成等距特征矩阵)                        |
|       [Step 03] 模型分类与回归训练 (生成或更新标准化模型包)                                  |
|       [Step 04] 离线全量预测与校正层评估 (产出训练评估指标)                                  |
|       [Step 05] 独立推理执行与差异漂移监测 (输出生产预测表及日级质量监控表)                  |
|       [Step 06] 事件段级行为深度分析 (对启动事件序列、周期指标与极限值做量化汇总)            |
+-----------------------------------------------------------------------------------------------+
```

---

## 第二部分：通用数据输入结构与规范

### 2.1 双重目录组织结构支持
为适应自动化生成数据与历史存量数据，框架原生支持两种目录与映射管理规范：
1. **规范化双分层目录布局（推荐）**：
   - 训练候选数据根目录：`data/trains/<device_id>_<user_id>/`
   - 推理独立数据根目录：`data/infers/<device_id>_<user_id>/`
   - *约定*：在此模式下，训练数据与推理数据处于不同物理目录；每个设备/对象目录内部需且仅需放置一组关联的输入源文件（输入主特征 CSV + 真值目标 CSV）。
2. **根目录平铺文件名正向推导模式（向后兼容）**：
   - 将所有相关数据 CSV 直接放置在 `data/` 根目录下，框架使用正则表达式识别设备 ID（`<device_id>`）、对象 ID（`<user_id>`）和信道属性，实现关联。

### 2.2 主特征输入 CSV（Bus / Main Feature CSV）标准规范
- **通用命名规范（`RE_BUS` 匹配语法）**：
  ```regex
  ^e241_(?P<device>[^_]+)_(?P<user>[^-]+)-Ch(?P<ch>\d+)-(?P<start>\d{6})-(?P<end>\d{6})(?P<suffix>(-1|-infer)?)\.csv$
  ```
  *字段解析*：`device` 表示设备或网关标识；`user` 表示受管理的对象或账户主键；`ch` 表示输入特征组通道号；`start` / `end` 表示起止日期。
- **通用数据格式约束**：
  - **主键列**：时间戳字段（可定名 `event_time` 或 `time`），支持 ISO 标准及不同分隔符的 `YYYY-MM-DD HH:MM:SS` 时间字符串，框架具备混合格式解析能力。
  - **数值特征列**：一列或多列用于模型学习输入的数值序列（如通道测量值 `load_iden_data*` 或多特征量）。

### 2.3 目标值真值 CSV（Branch / Target CSV）标准规范
- **通用命名规范（`RE_BR` 匹配语法）**：
  ```regex
  ^(?P<user>[^-]+)-(?P<start>\d{6})-(?P<end>\d{6})(?P<suffix>(-1|-infer)?)\.csv$
  ```
- **通用数据结构约束**：
  - **主键列**：时间戳字段 `time`，由重采样步骤与主特征 CSV 做 Inner/Left 对齐。
  - **目标实体列**：标量或各个具体分离维度目标列（命名为 `pN`，其中 N ≥ 0 的整数，如 `p1`, `p2`, ...）。
- **通用复合列物化机制（Composite Target Column, v13.16）**：
  - **业务场景**：当待建模目标是由多个细分目标求和形成的主体对象时，系统不要求必须在文件内手工生成汇总列，支持传入带 `+` 号的复合表达式。
  - **配置语法**：`"pA+pB[+pC...]"`（如 `"p1+p2"` 或 `"p1+p2+p3"`）。
  - **按需生成与计算法则**：数据加载函数根据请求自动进行列表解析并校验组件合法性；若均合法，则新增一列名称即为该表达式字面量（如 `p1+p2`）；计算值为行级数值累加（求和时严格设置 **`skipna=False`**，即任一组件在该时间点为 `NaN`，则复合结果将传播为 `NaN`，确保未采集区间绝不被静默设为 `0` 从而导致目标失真）。
  - **防呆容忍与拦截**：自动去除首尾空白及忽略字符大小写（`" P1 + p2 "` → `"p1+p2"`）；若传入重复分量（如 `"p1+p1"`）则捕获抛错予以拒绝。

### 2.4 中间态与对齐网格数据集
- 合并阶段：训练链路生成 `merged_bus.csv` / `merged_branch.csv`；推理链路生成 `infer_bus.csv` / `infer_branch.csv`（训推分离，防止泄漏）。
- 对齐矩阵：经网格化重采样及处理后生成 `aligned_15min.csv`（默认为 15 分钟间距的标准化时间序列矩阵）。

---

## 第三部分：通用数据输出与标准产物体系

### 3.1 产物目录分类体系
```
<workspace_root>/
 ├── models/                            # [持久化包资产区] 不会被单进程管道删除
 │    └── <user_id>/
 │         ├── model_bundle_v6_15_0.pkl # 包含归一化器、主模型、校正模型与元数据的综合捆绑包
 │         ├── rf_model.pkl             # 对照模型/基准模型包
 │         ├── split_dates_bundle.json  # 记录已分配至 train/val/test 日期集合的元数据清单
 │         └── weather_cache/           # 协变量气象等本地缓存支持
 └── artifacts/                         # [执行过程产物区] 包含白名单保护层与双分层子目录
      ├── batch_execution_state.csv     # (白名单保护) 原子更新的断点续跑 9 列状态表
      ├── batch_run_summary.csv         # (白名单保护) 任务执行的总体质量报表
      ├── summary_metrics_all_users.csv # (白名单保护) 全员聚合统计表
      ├── skipped_users.csv             # (白名单保护) 软跳过名单
      ├── trains/
      │    └── <user_id>/
      │         ├── train_on_periods.csv       # 训前全事件行为段级详表
      │         ├── train_on_periods_daily.csv # 训前全事件行为每日统计表 (含背景基线日)
      │         └── temp_power_lut.csv         # 训练侧 20 桶协变量-目标参考基底表
      └── infers/
           └── <user_id>/
                ├── predictions/
                │    └── inference_result.csv  # 完整多模型比对预测表
                ├── metrics/
                │    ├── inference_metrics.csv       # 全体指标评估表
                │    ├── inference_daily_metrics.csv # [25列标准] 逐日追踪与源数据采样密度监控表
                │    └── inference_comparison.csv    # 多模型对齐对比表
                ├── infer_on_periods.csv             # 推前事件段级明细
                ├── infer_on_periods_daily.csv       # 推前事件日级统计
                └── inference_temp_power_actual_vs_expected.csv # 实测分箱与预设分布相对漂移表
```

### 3.2 最终执行输出预测表 (`predictions/inference_result.csv`) 标准架构
用于持久化该用户的真实输出与多层结构算法估计，列结构定义为：
| 列名 | 类型 | 功能说明 |
|---|---|---|
| `time` / `event_time` | ISO String | 等距统一序列时间戳 |
| `y_true` | Float | 真实给定的标准目标值（未标注推理场景可缺省） |
| `y_pred_W_main` | Float | **主模型处理管道最终输出值**（经历原始预测 + 误差残差修正 + 后处理后的业务终态输出） |
| `y_pred_W_main_raw` | Float | 原始回归模型直接输出结果（保留最基础判断，供对比） |
| `y_pred_W_main_L4_calib` | Float | 仅施加加性残差校正层（L4）调整后的数值 |
| `state_pred_main` | Int (0/1) | 分类模型对目标是否处于触发/运行态（Active State）的二值判断 |
| `p_on_main` | Float (0~1) | 判定当前目标触发的分类预测特征概率 |
| `y_pred_low_W_main` / `y_pred_high_W_main` | Float | 不确定性预测区间上下边界（可选） |
| `residual_W_main_raw` / `residual_W_main_L4_calib` | Float | 原始及校正后计算对真值的绝对残差 |
| `y_pred_W_rf` ... | Float | 其他参考模型或基准基线对比结果列 |

### 3.3 通用逐日评估与输入审计表 (`train_daily_metrics.csv` / `inference_daily_metrics.csv`)
本规范为解决“**如何准确区分是算法精度大幅衰减还是上游数据原文件采集漏数/断点**”这一工程难点，确立了 **25 列标准化评估与数据审查规范**：

| 顺序索引 | 字段名 | 类型 | 通用涵义与应用价值 |
|---|---|---|---|
| 1 | `date` | String | 具体观察日期 (`YYYY-MM-DD`) |
| 2 | `split` | String | 该时间区间在处理逻辑上的归属 (`train` / `val` / `test` / `inference`)；若推理集与训练范围重叠，自动拆为 `inference_leak`（数据泄漏区）和 `inference_ood`（合规 OOD 区域）两行以示分离 |
| 3 | `model` | String | 模型标签标识（如 `main` 或 `rf`） |
| 4 | `n_samples` | Int | 本日经等距离采样及网格对齐后所得到的有效总样本行数 |
| 5 | **`n_bus_raw`** | Int / `""` | **[审计特化·源表监控] 对应此日期内，在上游主特征原始 CSV 文件里所能够真实读取到的条目总数**（若发生上游漏采或总记录数远低于 288 条 15 分钟满载值，将明确标识该日低指标为源端非稳态造成的异常） |
| 6 | **`n_branch_raw`** | Int / `""` | **[审计特化·源表监控] 对应此日期内，在目标/真值原始 CSV 中实际具备的实体总条数** |
| 7~11 | `Accuracy`, `Precision`, `Recall`, `F1`, `AUC` | Float | 事件分类判定的性能指标体系（保留 4~6 位小数） |
| 12~14 | `MAE_W`, `RMSE_W`, `SAE` | Float | 连续估计回归误差；其中 `SAE`（标准累积绝对误差）反映该区间总能量量的总体偏差度 |
| 15~17 | `kWh_true`, `kWh_pred`, `kWh_err` | Float | 该日累计能量/总贡献量的对应关系（标准总计值 / 预测终态值 / 累计绝对残差差值） |
| 18~21 | `TP`, `FP`, `FN`, `TN` | Int | 逐日分类混淆矩阵事件总计数 |
| 22 | `dataset` | String | 指明本数据行在划分体系上的来源（辅助区分 `used`/`excluded` 等状态） |
| 23 | `on_thr_w` | Float | 用于将连续回归值转换为事件判定（Active State）的基础临界阈值 |

### 3.4 周期性事件段级统计表 (`<stage>_on_periods.csv` & `_daily.csv`)
对于发生间歇式激活和事件切换的目标，系统不仅统计总数字，还需提供对阶段运行过程的具体描述：
- **段级详表 (`<stage>_on_periods.csv`)**：
  - 提取数据中的各个连续活动触发阶段（Active State Segment），提供表结构：`being_time`（起始秒级时间）、`end_time`（结束时间）、持续分长 `duration_min`、运行期间期望均值 `mean_w`、瞬时极值 `peak_w`，以及事件累计总能量 `energy_kwh`。
  - **最小极值字段 (`min_w`，v13.16 扩展)**：专门收集某段连续活动期间输出的“最小瞬时记录值”，方便对连续工作过程中的低位波动边界进行审计，严格保持物理法则 `min_w <= mean_w <= peak_w`。
  - **背景静默基线行 (OFF Day Row，v13.11 扩展)**：当某整天的候选窗口并未激发任何激活态时，系统自动插入一条静默记录行（其中 `duration_min=1440`，相关平均值以全日基线背景值为统计值），确保持久化时全周期无一日脱漏。
- **每日事件摘要表 (`<stage>_on_periods_daily.csv`)**：
  - 汇总给出每日整体视角的监控统计：`date`、开机段总频次 `n_segments`（静默日默认为 0）、有效持续时分 `total_on_min`、累计小时 `total_on_hours`、首条激发时间 `first_on_time`、末次停止时间 `last_off_time` 及各状态的联合代表值。

### 3.5 协变量参考基底表 (`temp_power_lut.csv`) 与偏差状态表 (`inference_temp_power_actual_vs_expected.csv`)
为有效探测当外界协变量变化（如温度、环境参数）对预测造成的目标分布漂移，定义以下结构：
- **协变量分桶参考表 (`temp_power_lut.csv`)**：
  - 将关键协变量划分成 20 个同宽区间（Bin 1 ~ Bin 20），外加一个全量兜底桶 `ALL_MEDIAN`（共 21 行）。
  - 各行提供：分桶编号 `bin_id`、下界 `temp_lo`、上界 `temp_hi`、目标预期分布中位数 `expected_signal`、桶内采样数 `n_samples`、均值/标准差/25分位数/75分位数及标识 `is_global_median`。
- **动态协变量分布对比表 (`inference_temp_power_actual_vs_expected.csv`)**：
  - 用推理实测样本向上述 20 桶对应，给出实测中位数，计算 `abs_residual` 与相对漂移率 `rel_drift`。
  - 标注漂移状态指示标记 **`drift_flag`**：
    - `OK`：漂移量处于可接受稳态范围。
    - `WARN`：漂移幅度显著变化（绝对值超过既定边界，如 ≥15%）。
    - `ALERT`：存在极大异常分布偏离（绝对值超过严重警戒阈值，如 ≥30%）。
    - `NO_DATA`：新推理区间未落入此分桶范围。

### 3.6 批量原子断点续跑状态表 (`artifacts/batch_execution_state.csv`)
- **设计初衷（v13.17 扩展）**：在面向成百上千批次的数据处理中，避免系统因挂断、异常断电导致全部进度作废。
- **双保险文件操作语义**：执行对 `batch_execution_state.csv` 的写入时，统一采取“先在内存写就 `batch_execution_state.csv.tmp` 临时文件 -> 操作完成调用标准无缝原子系统替换指令 (`os.replace`) 覆盖原文件”的方式；若程序意外发生宕机，永远能保证主文件结构完整一致，**最多只损失当前正被处理的唯一条目**。
- **9 列标准化协议字段 (UTF-8-BOM)**：
  1. `user_id`：受管理的任务主键或用户字符串
  2. `status`：最终结论标签（`ok` / `fail` / `soft_skip`）
  3. `success`：成功布尔标量（`True` / `False`）
  4. `started_at`：进入任务序列的时间点
  5. `finished_at`：任务处理终止并写入的时间点
  6. `duration_s`：当前节点耗时时分长
  7. `message`：执行状态附加摘要说明（含中文说明无编码异常）
  8. `target_col`：经过合法性处理及复合求和分析确认后的目标表达式（如 `p1+p2`）
  9. `run_id`：将状态记录绑定到当前调度执行的批量轮次码

---

## 第四部分：通用用户配置规范体系 (JSON Configuration Framework)

框架通过 `--time-filter-config <path_to_json>` 接受一份集中化的 JSON 配置清单。任何需要控制某具体对象或全局执行逻辑的改动，均通过配置而非硬编码修改代码来实现。

### 4.1 三重配置优先级覆盖与下划线保护链
```
参数解析引擎按优先级自上而下检索策略：
  1. config["<user_id>"]["<field>"]   —— 最高优先 (专门为该主键 ID 配置的具体设定)
  2. config["_default"]["<field>"]    —— 次高优先 (全局统一模版兜底，所有未指定对象享用)
  3. 代码层内置的常数默认兜底值      —— 保底生效
```
> **预留名称空间安全保障**：所有以单下划线开头命名的 JSON 属性名称（包含但不限于 `_note_`、`_comment_`、`_default`、`_v135_full_example_`）一律视作注释与框架元声明，调配器自动跳过，保证不会误判为真实受控对象主键。

### 4.2 六大标准功能配置层规格详解

#### [模块 1] 顶层训练/推理时段全局白黑名单裁剪 (`train` / `infer`，v12 增强)
- **通用功能**：在一个处理对象的数据集加载阶段，对允许进入管道的有效数据进行指定时间范围的截取。
- **属性声明**：`"train"` 与 `"infer"` 字典分别开放 `"include"`（被容纳的闭区间数组）和 `"exclude"`（需移除的区间数组）。
- **时间范围语义**：
  - 数组元素形如 `[start_str, end_str]`，表示**闭区间** `[start, end]`。
  - 支持传入短日期 `"YYYY-MM-DD"`，解析时按全天自动扩界（左边设为当日 `00:00:00`，右边充为当日 `23:59:59`）。
  - 支持带秒精确区间 `"YYYY-MM-DD HH:MM:SS"`，用以剔除局部几小时的断裂、噪声或不可信片段。
- **运算执行算法**：
  - 第一步：检查 `include` 并集列表；若存在，只保留区间并集范围内的数据；若为空，默认表示选取当前数据集的整个历史全貌。
  - 第二步：基于上面的选定行，执行 `exclude` 黑名单查杀，凡落入任一排除区间的时间点立即从分析集中删除。

#### [模块 2] 目标列定义与通用复合目标 (`target_col`，v13.4 / v13.16)
- **通用功能**：指导下游模型去加载目标 CSV 里哪一个具体列或哪些复合列来做真值标注。
- **支持模式**：
  - `"pN"`（单一目标名称，其中 N ≥ 0 的数字，如 `"p1"` 或 `"p10"`）。
  - `"pA+pB[+pC...]"`（任意多个带 `+` 串联的目标名，如 `"p1+p2"`，指示框架调用通用复合计算逻辑将其实时展开汇总成新的合法列）。
- **系统解析与容错次序**：若 JSON 内未声明此项，框架执行三阶段智能探测：优先检索数据输入表正向匹配正则取得的指定信道；若无明确信息，则读取首个列头中的合规 `p*`；最终兜底默认为 `"p1"`。

#### [模块 3] 业务守卫与自适应限制策略控制 (`guard_enabled`，v13.1 / v13.5)
- **通用功能**：控制业务侧针对突发浪涌或特殊工作模式启用的平滑过滤防守策略开关。
- **三态配置值与运行行为**：
  - `true`：显式强制打开业务守卫规则。
  - `false`：显式强制关闭规则；适用场景如在某些特殊低幅输出或稳定调频运行的主干设备中，避免守卫过度介入把合规工作态判为噪声所引起的召回率下降。
  - **`null` 或省略（未声明）**：走框架自带的**特征驱动自适应判定规则**。处理引擎会自动计算当班训练集中关键脉冲密度的最大可能极值和触发率；如果发现数值长期偏低（例如未达到阈下限或触发时间不到 30%），框架记录日志并**自动降级将其关停**。

#### [模块 4] 数据集细粒度分区独立过滤 (`splits`，v13.2)
- **通用功能**：深入至通过切分策略（如按天分层切分）已经产生的 `train`、`val` 和 `test` 划分集中，对指定区间执行强制修正。
- **数据规范**：开放 `splits.train`、`splits.val`、`splits.test` 节点，其下同样开放 `include` 和 `exclude` 指令。
- **四步严格自洽重新分配算法**：
  1. **原始标准划分 (Step 1)**：按照既定的数据集切分算法（例如 `stratified_day`）生成最基础的数据下标池。
  2. **强制硬锚定 (Step 2 - Include)**：检索各个划分区的 `include` 时间清单，凡命中的样本一律硬行归属该切分区。针对同一个点同时存在对多个不同切分池包含的冲突时，采取优先级 `train -> val -> test` 保障，排期在前的率先锁定。
  3. **重新校准与跨集形状恒久保护 (Step 3 - Balance)**：因为强制包含会令对应集膨胀，为防止破坏最初预设的比例（例如 60% / 20% / 20% 的三态比例），自动将在该集中未受到“强制硬锚定”的冗余普通记录行让出并转交给样本缩减的目标切分集，实现零差平移。
  4. **特定区排除与重分配池 (Step 4 - Exclude)**：剔除已在指定区间归入但又落入相应 `exclude` 的数据；若其仅被某具体阶段屏蔽，自动将其退还到普通候选点以备其他切分集吸纳；若同时命中三个独立区的 exclusion，将予以完整移除。

#### [模块 5] 核心工程公共参数强行覆盖 (`common_overrides`，v13.5 / v13.13)
提供从用户级直接覆写系统全局关键常数的通道，支持以下 9 项控制属性：
| 参数全称字段名 | 数值约束区间或类型 | 面向业务控制作用 | 异常检验失败时的处理行为 |
|---|---|---|---|
| `on_thr_w` | Float (`[0.001, 5000.0]`) | 业务目标是否处在启动/活跃状态的连续判定绝对临界阈值 | 发出 `UserWarning` 警告，回退为系统常规默认 (`10.0`) |
| `split_ratios` | 3 元素 Float 数组，总和=1 | 划分 `train/val/test` 的三个百分比数值（如 `[0.7,0.15,0.15]`，和不为 1 自动线性归一） | 校验有误时跳过并重置至内置预设 (`[0.6, 0.2, 0.2]`) |
| `split_strategy` | `"stratified_day"` / `"stratified"` / `"time"` / `"global_stratified"` | 划分采样的核心控制机制。**强力建议**优先选择 `"global_stratified"`，在跨月及非整段采样下直接面向整体天数抽样，彻底消除按月硬分层中小月零散天数过度向 `train` 倾斜的问题 | 回退至 `"stratified_day"` |
| `post_min_on` | Int (`>= 0`) | 事件状态识别时序平滑后处理：要求一次有效触发持续的最短周期步数 | 忽略错误设为 `1` |
| `post_fill_short_off`| Int (`>= 0`) | 事件状态识别时序平滑后处理：需予以连接补齐的最短间断停顿步长 | 忽略错误设为 `3` |
| `weather_latitude` | Float (`[-90.0, 90.0]`) | 设备对应观测所在城市的地理定位纬度，给气象类协变量 API 抓取用 | 兜底坐标纬度 `30.59` |
| `weather_longitude`| Float (`[-180.0, 180.0]`) | 对应城市的地理经度 | 兜底坐标经度 `114.31` |
| `use_weather_features`| Bool (`true`/`false`) | 是否在训练特征构建和模型特征域内开通温度与气象特征提取功能 | 默认保持开启 (`true`) |
| `use_temp_based_season`| Bool (`true`/`false`) | 是否开启根据协变量分布自动做时间序分层多模型选择（Seasonal Routing） | 默认保持开启 (`true`) |

#### [模块 6] 高级处理与进阶特征控制属性 (`v14_flags`)
面向模型扩展流水线，向底层进程提供对高阶特性启用状态的强类型规范表达：
```json
{
  "v14_enable": true,
  "physics": true,
  "focal": false,
  "ensemble": true,
  "calibrate": false,
  "auto_config": false,
  "health": true,
  "diag": true
}
```

---

## 第五部分：底层控制链路与核心安全保护契约

### 5.1 CLI 传递与环境变量封装解析链路
批处理管理引擎解析完成目标信息与参数层级后，向具体的单对象任务命令实现完全无感的双层分发：
1. **JSON 参数序列化调用**：
   - 将处理好的各时段 `include/exclude` 过滤定义序列化为紧凑 JSON，通过 `-time-filter-spec '...'` 与 `-splits-time-filter-spec '...'` 透传向所有内部处理脚本（`02_align_and_feat.py` ➔ `03_train.py` ➔ `05_inference.py`）。
2. **大写全大写环境变量配置封堵 (`NILM_USER_*`)**：
   - 针对 `common_overrides` 参数与开关字段，批量层将其自动转换为特定命名空间前缀的环境变量并压入子进程执行环境：
     ```bash
     NILM_USER_ON_THR_W=50.0
     NILM_USER_SPLIT_RATIOS=0.7,0.15,0.15
     NILM_USER_SPLIT_STRATEGY=global_stratified
     ```
   - *通用代码实践*：各个基础层逻辑代码在声明系统常量时必须设计对环境变量的高级监听：
     ```python
     ON_THR_W = float(os.environ.get("NILM_USER_ON_THR_W", default=10.0))
     ```
   - 实现无污染、高扩展性的配置解耦。

### 5.2 临时产物安全隔离契约 (`_CLEANUP_WHITELIST`)
为防止执行单设备/对象流水线任务对残留于 workspace 的临时垃圾文件作扫清（Garbage Collection）操作时，引发批调层核心状态误删的问题，框架明确了**顶部持久化文件白名单契约 (`_CLEANUP_WHITELIST`)**。

单任务层 `cleanup_artifacts_top(project_root)` 在对 `artifacts/` 顶层运行环境做重置和清除各种未命名的 `*.tmp` / `aligned_*.csv` 中间对齐表时，**以下核心主控文件和子系统永久分层必须被严格豁免而绝对禁止被误删**：
- `batch_execution_state.csv` —— 指引多任务调度的 **9 列标准通用执行进度表**（支持 `-resume` 跨轮续跑的核心断点资产）
- `batch_execution_state.csv.tmp` —— 处于操作系统文件系统原子级写入当中的备份文件
- `batch_run_summary.csv` —— 最终生成的全体多对象业务精度总全景表
- `summary_metrics_all_users.csv` —— 全量多模型及对比测试汇总指标库
- `skipped_users.csv` —— 经过配置被判断策略跳过的对象清点审计文件
- `.gitkeep` —— 仓库基础文件夹定位锚

### 5.3 严格错误边界与优雅降级控制规则 (`WARN + Fallback`)
1. **智能参数兼容强转**：
   - 为减少因为字符串形式或者跨语言格式导致的验证失败，所有数值与布尔接收均支持智能转换处理（如字符串 `"50"` 提升为浮点数 `50.0`；布尔字面量 `"yes"`, `"1"`, `"True"`, `"on"` 都将准确强转为有效 `True`）。
2. **非崩溃安全报错与系统默认降级**：
   - 为确保在线批量任务在部分对该对象的配置项拼写或超界异常时（如将阈值设为负数，或把参数三元划分之和配成非法值）能维持处理，程序针对所有验证异常都会通过标准 `UserWarning` 日志输出进行告警。
   - 抛出警报后，系统自动以“非侵入式”操作丢弃该处错配输入，自动继承通用层的可靠内置默认值，完成后续特征生成与算法推演。
